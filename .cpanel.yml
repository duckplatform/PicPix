---
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/picpix/
    - /bin/rm $DEPLOYPATH/* -Rf
    - /bin/cp -R * $DEPLOYPATH
    - source $HOME/nodevenv/picpix/24/bin/activate && cd $DEPLOYPATH
    - npm install
    - mkdir -p tmp
    - touch tmp/restart.txt
